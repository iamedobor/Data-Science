    services:
  - type: web
    name: trade-gdp-dashboard
    env: python
    rootDir: trade-gdp-analysis
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn dashboard:server --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.7
    plan: free    